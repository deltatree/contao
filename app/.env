# Contao Environment Configuration
# In production, set these via environment variables or .env.local

APP_ENV=prod
APP_SECRET=change_me_in_production

# Database connection (override in .env.local or via environment variable)
# DATABASE_URL="mysql://user:password@host:3306/database?serverVersion=mariadb-10.11.0"

# Trust all proxies for X-Forwarded-* headers (HTTPS behind reverse proxy)
# Use REMOTE_ADDR to trust the immediate connecting IP, or specify IPs/CIDRs
TRUSTED_PROXIES=REMOTE_ADDR
